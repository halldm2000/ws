# ws

Landing page for the Worldscope conference poster QR code.

The printed QR encodes <https://halldm2000.github.io/ws/> and can never change.
This page is what it resolves to, so **edit `index.html` to re-point it** —
the link targets are marked with comments. Commit and push; live in about a minute.
